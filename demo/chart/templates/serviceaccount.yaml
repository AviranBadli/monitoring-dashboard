{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "kueue-dashboard.serviceAccountName" . }}
  labels:
    {{- include "kueue-dashboard.labels" . | nindent 4 }}
{{- end }}
